# Test video index

## 展示网页

`showcase/` 提供面向评审的测试放映页：默认展示每个场景的最新录像，并附带中文测试说明、功能说明、运行指标和历史录制。录像完成后 `record_tests.ps1` 会自动刷新页面索引，也可以单独执行：

```powershell
.\tools\generate_test_video_showcase.ps1
python -m http.server 10086
```

然后访问 `http://localhost:10086/test_videos/showcase/`。浏览器出于安全限制不能在直接打开本地 HTML 文件时读取 JSON，因此需要一个本地 HTTP 服务。

仓库保留覆盖 75 个黑盒测试的规范录像，统一使用 AV1/WebM 并由 Git LFS 存储。历史上重复录制的早期批次已通过 `.gitignore` 排除。

85 段既有 AVI 已用 `libsvtav1 CRF 32 / preset 8` 迁移：3,309,160,498 字节降至 228,515,601 字节，保留 6.91%，逐段验证 codec、分辨率和帧数一致。迁移报告见 `compression_report.json`。

- `20260710_163708/`：最初 14 个基础移动、命令、避让、边界、单向狭口和 192 单位场景。
- `20260710_165242/`：动态建筑绕行、移除恢复和动态 Portal 改道。
- `20260710_165446/`：动态建筑局部路径失效。
- `20260710_171455/`：平衡、非对称和连续波次双向狭口。
- `20260710_171922/`：Hold 单位堵塞狭口与释放恢复。
- `20260710_191145/`：跨命令共享目标、临时包围恢复和永久不可达上限。
- `20260710_195020/`：受干扰后的终点收敛，以及 48 单位活动 Portal 失效后的群组共享改道。
- `20260710_200940/`：Godot Navigation Resource 转纯 C# 快照并驱动 Portal/Choke 运行时。
- `20260711_184359/`：版本化 Building Type Resource 驱动四种实际建筑的扣费、施工、人口与 Refinery 生命周期。
- `20260711_190740/`：生产队列、人口预留、十二出口封锁等待、恢复出生、Rally、单位死亡释放人口和生产建筑摧毁清理。
- `20260711_194814/`：Production Log、Package v5、checkpoint，以及生产中/出口等待/出生人口账本三种 Hot Snapshot v5 恢复。
- `20260711_200053/`：Production Catalog Godot Resource 驱动 Marine、Marauder、SCV 的战斗/Worker Profile 与生产经济结果。
- `20260710_204325/`：终点 V2 的临时阻挡重新收敛，以及外圈先就位、内部预留后释放的专用场景。
- `20260710_213409/`：加入主动 Yielding 后重新录制终点收敛和外圈封闭场景；分别触发 4 次和 8 次让路，结束时均无活动让路状态。
- `20260710_221417/`：A2 最终批次；终点收敛、外圈封闭、速度超车局部重匹配和角落混合半径共四段录像，全部通过。
- `20260710_224421/`：Clearance 第一层；Small/Large Portal 分流和动态建筑 24px 窄缝判定，全部通过。
- `20260710_224735/`：修正混合半径夹具括号后，重新录制 30 单位混合半径与 80 单位角落收敛，全部通过。
- `20260710_225930/`：四档建筑 footprint、业务放置规则和 24 单位多尺寸建筑群绕行，全部通过。
- `20260710_231548/`：Godot Gameplay Profile Resource 转纯 C# 快照并驱动 3 种单位、4 种建筑，全部通过。
- `20260711_000914/`：编辑器多尺寸净空预览；显示 3 档净空、5 条 Portal 和 4 档建筑 footprint，302 帧自动验收通过。
- `20260711_002932/`：全局 Connectivity Guard 与新版编辑器预览；唯一通道封闭被拒绝、8/8 单位通过，预览显示三档连通分量，两个场景均通过。
- `20260711_010831/`：版本化 Clearance Bake Resource 与新版编辑器预览；画面显示 `StaticBake` 数据源和 5×3 chunk 网格，两段录像均通过。
- `20260711_022211/`：AttackMove 最小闭环；接敌击杀后恢复、leash 脱战恢复、Move/AttackMove 隔离和 Stop/Hold 取消，共四段录像全部通过。
- `20260711_030216/`：战斗移动 D2；重录四条 AttackMove 基线，并加入近战 8/8 唯一接触槽、远程 10/10 攻击环、Stop/Hold 索敌语义和 8 对 4 连续重选敌，共八段录像全部通过。
- `20260711_033409/`：操作层第一阶段；三段 Shift 路点、即时替换、16 条队列上限、6 人 Control Group 和 SmartCommand 连续语义，共五段录像全部通过。
- `20260711_090654/`：确定性命令日志与回放；7 条命令 byte-identical 重放得到相同状态 Hash，修改目标后在 Tick 360 检出首次分歧，共两段录像全部通过。
- `20260711_094134/`：Replay Package；从资源身份、8 个初始单位和 1 个初始建筑重建世界，再精确重放建筑放置/移除与单位命令，最终状态 Hash 一致。
- `20260711_110031/`：Checkpoint seek；从 Tick 240 恢复后继续到 Tick 720，后半段 17 个周期状态 Hash 与连续运行全部一致。
- `20260711_113232/`：状态 Hash v2 与活跃狭口 checkpoint；32 单位双向交通在 Tick 240 恢复，后续 19 个采样全部一致且无方向冲突。
- `20260711_113402/`：状态 Hash v2 基线重录；命令日志、分歧定位、Replay Package 和普通 checkpoint 共四段，替代对应的 v1 Hash 录像。
- `20260711_114744/`：直接热快照；Tick 240 深拷贝战斗、Shift 队列、路径、动态建筑和狭口运行态，不重演早期 Tick，恢复后的 17 个采样全部一致。
- `20260711_115954/`：持久化热快照；3,646 字节规范载荷 round-trip 后直接恢复，并验证版本、截断、Package 不匹配和正文篡改拒绝。
- `20260711_122843/`：操作表现第二阶段；同类型双击选择、SelectionFilter、边缘滚动、光标锚定缩放和编组双击镜头定位全部通过。
- `20260711_125649/`：解耦 Minimap；真实 Control 显示障碍、单位、四种 footprint 建筑和视口框，坐标往返、镜头定位与 SmartCommand 意图全部通过。
- `20260711_132406/`：新版录制流水线验收；`single-unit` 从临时 AVI 自动编码、验证并保存为 AV1/WebM。
- `20260711_135134/`：dirty-chunk 增量 Connectivity；橙色高亮 chunks 1/6，仅重采样 512/3,080 cells，加入/移除与全量拓扑严格一致，多 revision 输入明确拒绝增量路径。
- `20260711_141754/`：Resource Fresh Load 与原子差异；生成的 Navigation/Profile/Bake 变体整体加载，面板显示 1 个障碍、1 个单位 Profile 和 `RebuildSimulation` 策略，错配 Bake 被拒绝。
- `20260711_144424/`：Bake-only 两阶段安全提交；8 个单位移动途中原子替换 Grid/放置守卫缓存并全部重规划，错误导航哈希候选随后被拒绝，最终 8/8 到达。
- `20260711_145641/`：放置前后 Connectivity 差异；候选 footprint 封死唯一走廊，Small/Medium/Large 均从 1 个分量变为 2 个，面板显示 blocked/split/disconnected 与 dirty chunks，安全候选三档均保持连通。
- `20260711_151949/`：S9 文件监听闭环；2 次 Bake 写入通知经过去抖只触发 1 次 Fresh Load/原子提交，8 个移动单位全部重规划并 8/8 到达，面板显示 `Applied / Success / reloads=1`。
- `20260711_154903/`：S11-A 双资源经济；8 个工人在三个矿脉与一个 Refinery 间实际往返，第一矿脉枯竭后自动转场，20 秒获得 105 Minerals/48 Vespene，实时显示资源、人口、工作阶段与节点余量。

重新录制所有当前场景：

```powershell
.\tools\record_tests.ps1
```

录像脚本默认输出 AV1/WebM。首次运行自动下载经过 SHA-256 校验的 FFmpeg 到忽略目录 `tools/.cache/`；不会把 FFmpeg 二进制提交到仓库。编码与迁移细节见 `docs/VIDEO_RECORDING.md`。
